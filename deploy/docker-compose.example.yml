services:
  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      args:
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
    environment:
      DATABASE_URL: ${DATABASE_URL}
      LOCAL_STORAGE_ROOT: /app/data/uploads
      SUBMISSION_CLEANUP_SECRET: ${SUBMISSION_CLEANUP_SECRET}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      SMTP_HOST: ${SMTP_HOST:-smtp}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      AUTH_EMAIL_FROM: ${AUTH_EMAIL_FROM}
      INVITATION_CODE_PEPPER: ${INVITATION_CODE_PEPPER}
      CHALLENGE_FLAG_PEPPER: ${CHALLENGE_FLAG_PEPPER}
      INTEGRATION_ENCRYPTION_KEY: ${INTEGRATION_ENCRYPTION_KEY}
      EXTERNAL_SERVICE_ALLOWED_HOSTS: ${EXTERNAL_SERVICE_ALLOWED_HOSTS}
      NOTIFICATION_DELIVERY_SECRET: ${NOTIFICATION_DELIVERY_SECRET}
      WEB_PUSH_VAPID_PUBLIC_KEY: ${WEB_PUSH_VAPID_PUBLIC_KEY}
      WEB_PUSH_VAPID_PRIVATE_KEY: ${WEB_PUSH_VAPID_PRIVATE_KEY}
      WEB_PUSH_SUBJECT: ${WEB_PUSH_SUBJECT}
      INSTANCE_PROVIDER_URL: ${INSTANCE_PROVIDER_URL}
      INSTANCE_PROVIDER_TOKEN: ${INSTANCE_PROVIDER_TOKEN}
      AUTH_TRUST_PROXY: ${AUTH_TRUST_PROXY:-false}
    ports:
      - "${APP_BIND_ADDRESS:-127.0.0.1}:${APP_PORT:-3000}:3000"
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 256
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
      - /app/.next/cache:rw,noexec,nosuid,nodev,size=128m
    volumes:
      - local_storage:/app/data/uploads
    depends_on:
      migrate:
        condition: service_completed_successfully
      smtp:
        condition: service_started
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/api/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      app_network: {}
      mail_network:
        gw_priority: 1

  smtp:
    image: boky/postfix:v5.1.0-alpine@sha256:6e761312e833947f77af755072c0e77b67d298d127595470a79e9454008dd594
    hostname: ${SMTP_HOSTNAME:-mail.example.com}
    environment:
      ALLOWED_SENDER_DOMAINS: ${SMTP_ALLOWED_SENDER_DOMAINS:-mail.example.com}
      POSTFIX_myhostname: ${SMTP_HOSTNAME:-mail.example.com}
      POSTFIX_mynetworks: 127.0.0.0/8,${MAIL_NETWORK_SUBNET:-172.30.42.0/24}
      DKIM_AUTOGENERATE: "1"
      DKIM_SELECTOR: flowtask
    expose:
      - "587"
    volumes:
      - smtp_spool:/var/spool/postfix
      - smtp_dkim:/etc/opendkim/keys
    restart: unless-stopped
    networks:
      - mail_network

  migrate:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: builder
      args:
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
    command: ["sh", "-c", "node deploy/scripts/preflight-local-auth.mjs && npx prisma migrate deploy"]
    environment:
      DATABASE_URL: ${DATABASE_URL}
      HOME: /tmp
      NPM_CONFIG_CACHE: /tmp/.npm
    user: "1001:1001"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 128
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    depends_on:
      db:
        condition: service_healthy
    restart: "no"
    networks:
      - app_network

  db:
    image: postgres:17-alpine@sha256:742f40ea20b9ff2ff31db5458d127452988a2164df9e17441e191f3b72252193
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    networks:
      - app_network

volumes:
  postgres_data:
  local_storage:
  smtp_spool:
  smtp_dkim:

networks:
  app_network:
    driver: bridge
    internal: true
  mail_network:
    driver: bridge
    ipam:
      config:
        - subnet: ${MAIL_NETWORK_SUBNET:-172.30.42.0/24}
