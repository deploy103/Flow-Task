[Unit]
Description=Flow Task Docker ingress firewall
Requires=docker.service
After=docker.service
PartOf=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/flow-task-docker-firewall
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
